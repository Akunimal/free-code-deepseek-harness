{
  "name": "free-code-deepseek-harness",
  "version": "0.4.2",
  "private": true,
  "description": "Multiplatform desktop GUI shell for deepseek-harness powered by an opencode2api worker pool (DeepSeek-free first, zero-config)",
  "packageManager": "pnpm@11.22.0",
  "engines": {
    "node": ">=22.19"
  },
  "scripts": {
    "build": "pnpm -r --filter \"./packages/**\" --filter \"./apps/**\" build",
    "prepare:upstream": "node scripts/link-upstream-workspace-packages.mjs",
    "test": "pnpm prepare:upstream && pnpm --filter @freecode/opencode-adapter build && pnpm -r --filter \"./packages/**\" --filter \"./apps/**\" test",
    "test:contract": "pnpm prepare:upstream && pnpm --filter @freecode/opencode-adapter build && pnpm --filter @freecode/contract-tests test",
    "typecheck": "pnpm -r --filter \"./packages/**\" --filter \"./apps/**\" typecheck",
    "build:vendor": "pnpm --dir vendor/deepseek-harness build",
    "build:shell": "pnpm --filter @freecode/shell build",
    "package:runtime": "node scripts/run-package-runtime.mjs",
    "build:desktop": "pnpm build && pnpm package:runtime && pnpm --filter @freecode/shell package",
    "release:gate": "node scripts/release-gate.mjs"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "typescript": "^5.6.3",
    "unrun": "^0.3.1",
    "vitest": "^2.1.8"
  }
}
